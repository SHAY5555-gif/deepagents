# ============================================
# Core Dependencies - Order Optimized
# ============================================
# Most stable packages first for better caching
# If these don't change, Docker will reuse the cached layer

# Core web framework dependencies
fastapi==0.115.12
pydantic==2.10.6
httpx==0.28.1

# ASGI server with performance optimizations
uvicorn[standard]==0.34.0

# LangChain integration
langchain-xai
langchain-cerebras

# Vercel AI Gateway integration
openai>=1.50.0
python-dotenv>=1.0.0

# FireCrawl SDK for web scraping
firecrawl-py>=4.6.0

# BrightData SDK for enterprise web scraping and search
brightdata-sdk>=1.1.0

# Genius API for song lyrics
requests>=2.31.0

# Async utilities for nested event loops
nest_asyncio>=1.5.0
